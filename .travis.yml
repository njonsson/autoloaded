language: ruby
bundler_args: --without debug doc tooling
rvm:
  - 2.0
  - 2.1
  - ruby-head
  - jruby-head
script: "bundle exec rake test"
matrix:
  allow_failures:
    - rvm: ruby-head
    - rvm: jruby-head
